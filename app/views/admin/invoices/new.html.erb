<% content_for :title, @invoice.new_record? ? "Buat Invoice" : "Edit Invoice" %>

<div class="max-w-4xl mx-auto mt-10">
  <h1 class="text-2xl font-bold text-gray-800 mb-6">
    <%= @invoice.new_record? ? "Buat Invoice Baru" : "Edit Invoice" %>
  </h1>

  <%= render "form", invoice: @invoice %>

  <div class="mt-4">
    <%= link_to "Kembali", admin_invoices_path, class: "text-blue-600 hover:underline" %>
  </div>
</div>

