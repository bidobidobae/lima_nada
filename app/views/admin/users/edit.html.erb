<% content_for :title, "Edit Pengguna" %>

<div class="max-w-4xl mx-auto mt-10">
  <h1 class="text-2xl font-bold text-gray-800 mb-6">Edit Pengguna</h1>
  <%= render 'form' %>

  <div class="mt-6">
    <%= link_to "← Kembali ke Daftar Pengguna", admin_users_path, class: "text-blue-600 hover:underline" %>
  </div>
</div>
